//! Application layer: orchestrates domain logic.
//!
//! Implements driving ports (EntityOperations).
//! Uses driven ports (EntityRepository, Notifier) via dependency injection.

pub mod services;
pub mod dto;

pub use services::{
    CreateEntityUseCase,
    ListEntitiesUseCase,
    GetEntityUseCase,
    ArchiveEntityUseCase,
    ActivateEntityUseCase,
    RenameEntityUseCase,
    DeleteEntityUseCase,
    EntityService, // Main facade implementing EntityOperations
};
