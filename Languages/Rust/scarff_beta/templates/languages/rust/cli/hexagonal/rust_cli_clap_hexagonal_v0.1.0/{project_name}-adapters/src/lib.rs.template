//! Secondary (Driven) Adapters
//!
//! Implements the ports defined in scarff-core::ports::driven.
//! These adapters depend on the core, never the other way around.

pub mod persistence;
pub mod notification;
pub mod config;

pub use persistence::SqliteRepository;
pub use notification::ConsoleNotifier;
pub use config::FileConfigProvider;
