//! Domain layer: entities, value objects, and repository traits.
//!
//! This module contains the core business logic that is independent of
//! any external concerns (database, CLI, file system, etc.).

pub mod entity;
pub mod error;
pub mod repository;

pub use entity::Entity;
pub use error::Error;
pub use repository::Repository;
